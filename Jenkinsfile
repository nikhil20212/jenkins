pipeline {
   agent any

   stages {
      stage('Verify Branch') {
         steps {
            echo "$GIT_BRANCH"
            echo "$GIT_BRANCH"
            echo "$GIT_BRANCH"
         }
      }
   }
 }
